<section class="portlet sectionRelated" id="quip_search">
  <div>
    <h3>Search for topic</h3>
    <form class="form-vertical" action="">
      <div class="control-group">
        <div class="controls">
            <label class="text" for="show-only-tag">
            <input id="show-only-tag" class="input input-xlarge" type="text"
                   name="${layout.here_url}tag.html?tag=" />
            </label>
            <button class="btn" type="submit">Search</button>
        </div>
      </div>
    </form>
  </div>
  <div class="recent-topics">
    <h3>Recent topics</h3>
    <span class="badge ${'badge-following' if tag in followed_tags else 'badge-not-following'}" tal:repeat="tag tag_list">
      <a class="not-following" tal:condition="not tag in followed_tags" href="${chatter_url}add_followed_tags.html?add=${tag}"><i class="icon-ok icon-white"></i></a>
      <a class="following" tal:condition="tag in followed_tags" href="${chatter_url}remove_followed_tags.html?remove=${tag}"><i class="icon-remove icon-white"></i></a>
      <a href="${layout.here_url}tag.html?tag=${tag}">${tag}</a>
    </span>
  </div>
  <div class="followed-topics" tal:condition="followed_tags">
    <h3>Followed topics</h3>
    <span class="badge ${'badge-following' if tag in followed_tags else 'badge-not-following'}" tal:repeat="tag followed_tags">
      <a class="following" tal:condition="tag in followed_tags" href="${chatter_url}remove_followed_tags.html?remove=${tag}"><i class="icon-remove icon-white"></i></a>
      <a href="${layout.here_url}tag.html?tag=${tag}">${tag}</a>
    </span>
  </div>
  <script language="javascript" type="text/javascript">
    //<![CDATA[
    $(document).ready(function() {
        $('.searchButton').click(function(e) {
          $('#chatter-quips').load($('#show-only-tag').attr('name') + $('#show-only-tag').attr('value') + ' #chatter-quips');
          return false;
          $(".timeago").timeago();
        });
    });
    //]]>
  </script>
</section>
