# This suite is to functionality for calendar for this installation of karl.
# Set variables inside of calendar.conf
# run from this directory, by running
# flunc calendar
# see more options in the README

# have to login first
login.login

# make the community
community.make_community

# run calendar test suite
calendar.calendar-tests
