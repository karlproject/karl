[monitor]
hosts = localhost

[localhost]
description = Karl web server
port = 6543

[server]
# supposedly configuration used by monitord
host = localhost
port = 8008

interval = .5

# network interface to monitor lo, eth0
interface = lo

[client]
# supposedly configuration used by monitorctl
host = localhost
port = 8008