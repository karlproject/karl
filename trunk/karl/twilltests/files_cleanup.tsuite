# This is the cleanup routines for the "files" suite
# To run just the cleanup suite, use the -C flag
# flunc -C files
#
# or to run the "files" suite without running cleanup 
# use the -X flag
# flunc -X files
# See README.txt for more

# Cleanup tests
community.delete_testing_community