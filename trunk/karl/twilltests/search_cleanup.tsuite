# This is the cleanup routines for the "forums" suite
# To run just the cleanup suite, use the -C flag
# flunc -C search
#
# or to run the "forums" suite without running cleanup 
# use the -X flag
# flunc -X search
# See README.txt for more

# Cleanup tests
community.delete_testing_community